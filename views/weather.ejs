<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Weather Forecast</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(to bottom, #87CEFA, #f0f8ff);
            color: #333;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 30px;
        }
        h1 {
            color: #2c3e50;
        }
        form {
            margin-bottom: 20px;
        }
        input[type="text"] {
            padding: 10px;
            font-size: 16px;
            border-radius: 5px;
            border: 1px solid #ccc;
        }
        button {
            padding: 10px 15px;
            font-size: 16px;
            border-radius: 5px;
            border: none;
            background-color: #3498db;
            color: white;
            cursor: pointer;
        }
        button:hover {
            background-color: #2980b9;
        }
        .weather-box {
            background-color: rgba(255,255,255,0.8);
            padding: 20px;
            border-radius: 10px;
            max-width: 400px;
            text-align: left;
            box-shadow: 0 0 10px rgba(0,0,0,0.2);
        }
        .error {
            color: red;
        }
    </style>
</head>
<body>
    <h1>Weather Forecast</h1>

    <form action="/weather" method="POST">
        <input type="text" name="city" placeholder="Enter city name" value="<%= city ? city : '' %>">
        <button type="submit">Get Weather</button>
    </form>

    <% if (error) { %>
        <p class="error"><%= error %></p>
    <% } %>

    <% if (weather) { %>
        <div class="weather-box">
            <h2>Weather for <%= city %>:</h2>
            <p><%- weather %></p>
        </div>
    <% } %>
</body>
</html>
